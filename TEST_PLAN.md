# Yahoo! KeyKey arm64 — 測試計劃

> 用途：確認 arm64 轉譯版所有輸入法皆正常，作為 GitHub release 前的驗收依據。
> 原則：**測試與 debug 分離**。測試階段不載入 AI；只有失敗的 case 才帶最小重現 + 紀錄進入 debug。

## 1. 環境與前置

- 測試機：Apple Silicon（arm64）、macOS 15.0（Sequoia）以上
- 安裝來源：`YahooKeyKey-arm64.pkg`（或 .dmg）
- 安裝後：登出再登入；系統設定 → 鍵盤 → 文字輸入 → 編輯 → 啟用 Yahoo! KeyKey
- 對照組：原始 Intel / Rosetta 版 `YahooKeyKey.pkg`（行為基準，只做差異比對用）
- 測試 App：TextEdit（主要）、Safari、Mail、Terminal（次要）

## 2. 測試矩陣（每格：✔ 通過 / ✘ 失敗 / - 未測）

> 橫列為「測試情境」，直欄為「輸入法」。先填 ✔/✘，再於「結果記錄」補註解。

### 輸入法清單
- A. 傳統注音（Traditional Zhuyin）— 已開始使用，無異常
- B. 好打注音（SmartMandarin / 智慧注音）— 已知 >3 字選字卡頓+當機
- C. 倉頡（Cangjie）— 未測
- D. 簡易 / 速成（Simple）— 未測
- E. Canton（廣東拼音 / 粵語）— 未測

### 情境（每一列都要對每個輸入法跑）

| # | 情境 | A 傳統注音 | B 好打注音 | C 倉頡 | D 簡易 | E Canton |
|---|------|:--:|:--:|:--:|:--:|:--:|
| 1 | 啟用輸入法、選單列正確顯示、可切換 | | | | | |
| 2 | 單字基本輸入 | | | | | |
| 3 | 選字（1~5 候選、數字/空白/方向鍵選取、Tab/Enter 確認） | | | | | |
| 4 | 多字組字（2~3 字） | | | | | |
| 5 | 長句組字（>3 字）— **好打注音重點** | | | | | |
| 6 | 自動校正 / 上下文選字 | | | | | |
| 7 | 全形標點（shift+`,` → `，`、shift+`.` → `。` 等） | | | | | |
| 8 | 數字與特殊符號 | | | | | |
| 9 | 中英切換、Caps Lock、Shift | | | | | |
| 10 | Backspace 刪除 / 重新組字 | | | | | |
| 11 | 同音字、罕用字、多音字 | | | | | |
| 12 | 連續輸入長度壓力（10+ 字不卡頓不當機） | | | | | |
| 13 | 長時間使用記憶體穩定（無洩漏、無累積卡頓） | | | | | |
| 14 | 跨 App 一致（TextEdit / Safari / Mail / Terminal） | | | | | |
| 15 | 登出登入 / App 重啟後持久性 | | | | | |
| 16 | 輔助工具（Preferences、PhraseEditor，x86_64 走 Rosetta） | | | | | |

## 3. 結果記錄格式（測試時填寫，供 debug 引用）

每個失敗 case 記錄以下欄位，讓 debug 階段可以直接引用、不需重跑全矩陣：

```text
[case] 情境編號 + 輸入法（例：5-B）
輸入：<實際按鍵序列>
期望：<預期輸出>
實際：<實際輸出 / 現象>
重現性：<每次 / 偶發 / 只發生一次>
發生點：<哪個 App、哪一步>
效能：<卡頓時間、是否當機、Crash 類型>
相關檔案/紀錄：<崩潰 log 路徑、console log 路徑>
備註：<其他現象>
```

## 4. 測試 / debug 分離策略

### 4.1 測試階段（不載入 AI）
- 只照矩陣跑、填 ✔/✘，並填「結果記錄」的固定格式。
- 不追求根因；只記錄「現象」與「重現方式」。
- 產出物：`TEST_RESULTS.md`（或直接在此檔補填）。

### 4.2 Debug 階段（只在失敗時載入 AI）
- 只針對**單一失敗 case** 進行，且先備妥：
  1. 最小重現（最少的按鍵序列，能穩定重現）
  2. 崩潰紀錄（`~/Library/Logs/DiagnosticReports/` 或 Console.app）
  3. 相關 console / 系統 log
  4. 相關程式碼片段（若原始碼在手上）
- 這樣 AI 只需讀「該 case 的結果記錄」，不需重跑全矩陣 → 省 context/token。

## 5. 優先順序與 descope 決策

- **P0（Release 阻擋）**：
  - 5-B：好打注音 >3 字卡頓 + 當機（目前唯一已知 P0）
  - 3/4/5 之 B、C、D、E：選字與組字基本功能
- **P1（重要，可後補）**：自動校正、全形標點、跨 App、記憶體穩定
- **P2（次要）**：輔助工具（Rosetta 子進程）、持久性細節

### Release 條件（全部符合才釋出）
1. 所有輸入法：情境 1~5（啟用/單字/選字/組字/長句）皆 ✔
2. 好打注音：長句（>3 字）穩定不卡頓、不當機
3. 無 P0 等級的未解問題

### Descope 條件（若持續無法 debug 好打注音）
- 若 5-B（>3 字卡頓）在期限內無法解決：
  - 方案一：好打注音標註「已知限制：>3 字選字可能卡頓」，仍可 release
  - 方案二：僅 release 已驗證的輸入法（傳統注音、倉頡、簡易、Canton），好打注音標「beta」
- 判斷準則：**卡頓是否影響日常可用**。若「2~3 字正常、偶發長句卡頓」→ 可 descope 為已知限制；若「連 2~3 字都卡」→ 必須修。

## 6. 已知問題追蹤（好打注音 >3 字卡頓）

- 現象：選字超過 3 個字 → 卡頓 + 當機
- 初步假設（待驗證）：
  - a. 組字器 beam search 組合爆炸（每字多候選 → 候選空間指數成長）
  - b. 重建的 bigrams/unigrams 對長序列查詢異常（空指標 / 無窮迴圈）
  - c. 記憶體配置過大或洩漏（長序列累積）
- Debug 前置收集：崩潰類型（SIGSEGV / SIGABRT / CPU 卡死）、卡頓發生在「打字中」還是「選字框顯示」、最小重現序列（例如輸入哪幾個注音鍵）。

## 7. 待確認（與你討論）

- 好打注音 >3 字卡頓：是「輸入時就卡」還是「顯示選字框時卡」？當機是 App 崩潰還是系統層級？
- 原始碼是否在手上？（目前 repo 只有 artifacts，若無原始碼，debug 會較困難）
- 是否需要 AppleScript / 自動化腳本驅動按鍵 + 讀回文字，以減少人工測試？
